# AI_recovery_system

## Generator notes

`generate_batch` aims for ~18% technical / 82% business, then injects two issuer outage windows. Those windows push the observed technical share to **~28%** (seed 42: 99 `ISSUER_DOWNTIME` + 40 `NETWORK_TIMEOUT` = 139/500). That is expected, not a miss on the 18% target.
